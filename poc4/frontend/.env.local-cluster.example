# 6A local-cluster frontend: the Vite dev server proxies /api/v1 (HTTP + WS) to the local backend.
# All browser traffic is same-origin; no backend address reaches the bundle.
VITE_API_BASE_URL=/api/v1
VITE_USE_MSW=false
