# Manao 领域术语

Manao 把编辑、运行、反馈和继续修改连接为一个项目内的开发过程。这里定义产品概念；交付状态和技术方案以项目目标及对应设计文档为准。

## Language

**用户项目（Project）**：属于一个用户的开发单元，承载代码、环境配置、运行记录和项目依赖数据；其存在时间不由某次应用运行决定。
_Avoid_: 用“Pod”或“Job”代指整个项目。

**工作区（Workspace）**：项目中可浏览、编辑和保存的源文件集合及其保存版本。
_Avoid_: 把数据库内部数据文件称为工作区文件。

**应用运行（Run）**：用户基于一个已保存版本发起的一次执行会话，有独立标识、状态和日志；服务型运行可以持续提供访问直到停止或失败。
_Avoid_: 把一次运行的结束称为项目删除。

**任务型运行（Task run）**：以执行结束及退出结果作为完成依据的应用运行。
_Avoid_: 用它的成功结束语义描述持续在线的服务。

**服务型运行（Service run）**：以持续处理请求为目的的应用运行，需要区分启动、可访问、不可用和停止。
_Avoid_: 把进程已启动等同于服务已经可访问。

**项目依赖（Project dependency）**：为某个用户项目提供的 MySQL、Redis 等配套能力，与 Manao 自己使用的平台数据库分开管理。
_Avoid_: 用“后端数据库”同时指代平台数据库和用户项目数据库。

**项目数据（Project data）**：应用写入其项目数据库的数据；已经确认的 MySQL 数据保留要求覆盖停止应用、修改代码及再次运行。
_Avoid_: 把保留要求解释成应用不能自行修改数据，或对任意存储故障的恢复保证。

**公开端点（Public endpoint）**：用户明确选择公开的应用端口所对应的外部访问地址；地址分配与应用当前可访问是不同事实。
_Avoid_: 把已分配地址等同于在线，或把工作区管理接口称为应用端点。

**停止应用（Stop application）**：终止当前应用运行并释放后续编辑条件，保留项目、代码及项目数据库数据。
_Avoid_: 清空数据库、删除项目。

**删除项目（Delete project）**：用户明确请求永久回收该项目及其关联资源和数据的操作；未完成的回收不能报告为已删除。
_Avoid_: 停止、归档。
